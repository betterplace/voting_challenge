<%= form_with(model: donation, remote: false) do |form| %>
  <% if donation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(donation.errors.count, "error") %> prohibited this donation from being saved:</h2>

      <ul>
        <% donation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :amount_in_cents, style: "display: block" %>
    <%= form.number_field :amount_in_cents %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
